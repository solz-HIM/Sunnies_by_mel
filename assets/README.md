# Source assets (not served)

Files here are build/design sources kept out of `public/` so they are never
shipped to visitors.

- `logo-source.png` — original photo of the shop sign. `public/logo.png`,
  `src/app/icon.png` and `src/app/apple-icon.png` are all circular crops of it
  (crop box: left 25, top 20, 318×318). Regenerate them from this file if the
  branding changes.
